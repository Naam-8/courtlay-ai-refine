kind = "web"
previewPath = "/"
title = "Courtlay Website"
version = "1.0.0"
id = "artifacts/courtlay-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25884

[services.development]
run = "pnpm --filter @workspace/courtlay-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/courtlay-site", "run", "build" ]
publicDir = "artifacts/courtlay-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25884"
BASE_PATH = "/"
